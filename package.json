{
  "name": "marp-cli-example",
  "version": "0.0.0",
  "author": "David Yallup <dy297@cam.ac.uk>",
  "license": "WTFPL",
  "private": true,
  "scripts": {
    "build": "rimraf dist && npm run -s og-image && npm run -s deck",
    "deck": "marp --no-stdin dy_cavendish_hep.md --html -o dist/index.html && ncp assets dist/assets",
    "now-build": "npm run -s deck",
    "og-image": "marp dy_cavendish_hep.md -o dist/og-image.jpg",
    "start": "marp -ps ."
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^2.4.0",
    "ncp": "^2.0.0",
    "rimraf": "^3.0.2"
  }
}
